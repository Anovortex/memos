// Spaces stay hidden until the Teams gate exists (owner decision, 2026-09-23:
// "do not make it visible to everyone"). When packages unlock shared spaces
// this becomes a per-user check rather than a constant.
export const SPACES_ENABLED = false;
